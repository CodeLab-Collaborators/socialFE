export type iCon = {
  width?: string;
  icon: any;
  color?: string;
  size: string;
};

export type iProfile = {
  size?: string;
  avatar?: any;
};

export type iMore = {
  icon: any;
  text: string;
  size: string;
  iconColor: string;
  backgroundColor: string;
};

export type iConAndText = {
  icon: any;
  text: string;
  size: string;
  iconColor?: string;
  textColor?: string;
};
